var mongoose = require('libs/mongoose'),
    Schema = mongoose.Schema;

var schema = new Schema( {
    user_createid:{
        type:String,
        required: true
    },
    data:{
        type:Object,
    },
    created: {
        type: Date,
        default: Date.now
    }
});

exports.Org = mongoose.model('Org', schema);
